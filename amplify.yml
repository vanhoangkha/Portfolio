version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Building portfolio..."
        - echo "No build process needed - static HTML/CSS/JS"
  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths: []
