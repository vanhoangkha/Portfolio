'use client';

import React from 'react';

export default function BlogPage() {
	return (
		<main style={{ padding: '2rem', maxWidth: 960, margin: '0 auto' }}>
			<h1>Blog</h1>
			<p>Blog page placeholder. Content migration from the existing app is pending.</p>
		</main>
	);
}


